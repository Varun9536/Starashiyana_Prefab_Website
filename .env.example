# Copy this file to `.env` on the VPS before running `docker compose build`.
# NEXT_PUBLIC_* variables are inlined into the client bundle at BUILD time —
# changing this after the image is built requires a rebuild, not just a restart.

NEXT_PUBLIC_SITE_URL=https://starashiyanaprefab.com
